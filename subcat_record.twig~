{% extends 'partials/_master.twig' %}

{% block category %}
    <div class="categories">
    <div class="tiles">
        {% set subcats = record.related() %}
        {% for subcat in subcats %}
        <div class="tile">
                    <div class="tile-title"><a href="{{ subcat.link }}">{{subcat.title}}</a></div>
                    <div class="tile-img"><a href="{{ subcat.link }}"><img src="{% if subcat.image %} {{ subcat.image | thumbnail('tileimg') }} {% else %} {{ asset('images/default.png', 'theme') }} {% endif %}" alt="Illustration"></a></div>
                    <div class="tile-footer">
                        <a href="{{ subcat.link }}">{{ l('show-more') }}</a>
                    </div>
                </div>
        {% endfor %}
    </div>
    </div>
{% endblock category %}
