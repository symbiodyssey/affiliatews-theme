{% extends 'partials/_master.twig' %}

{% block banner %}
<div class="banner">{% setcontent logo = 'block/banner' %}
{% if logo.image is not empty %}
    <img src="{{ image(logo.image) }}" alt="{{record.values.banner.title|default('')}}" title="{{record.values.banner.title|default('')}}"/>
{% endif %}
</div>

<!-- YOU CAN DELETE FROM THIS LINE -->

<div style="background-color:#a6d4f4;text-aligh:center;padding:50px;">
<h1>Thanks for using the affiliate website template</h1>
<p>To take fully advantage of this template, a few simple steps are required and described in the documentation : <a href="http://bolt-affiliate-website-template.readthedocs.io/en/latest/configure_template.html" target="_blank">http://bolt-affiliate-website-template.readthedocs.io/en/latest/configure_template.html</a></p>
<p>Consider as well reading the following page to get an overview of the different website architectures that are available : <a href="http://bolt-affiliate-website-template.readthedocs.io/en/latest/website_architecture.html" target="_blank">http://bolt-affiliate-website-template.readthedocs.io/en/latest/website_architecture.html</a></p>
<p>To delete this message, in the dashboard in the left menu, go to File management -> View/edit templates -> affiliatews/ -> index.twig. In the text editor, you can delete this message.</p>
</div>

<!-- TO THIS LINE INCLUDED -->

{% endblock banner %} 

{% block category %}
    <div class="tiles">
        {% set tmpl = "partials/_product_mode#{config.get('theme/ws_mode')}#{config.get('theme/ws_ga')==1?'_ga':''}.twig" %}
        {% for item in records %}
            {% include tmpl %}
        {% endfor %}
    </div>
   {{ pager(template = 'partials/_sub_pager.twig') }}
{% endblock category %}
