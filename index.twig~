{% extends 'partials/_master.twig' %}

{% block banner %}
<div class="banner">{% setcontent logo = 'block/banner' %}
{% if logo.image is not empty %}
    <img src="{{ image(logo.image) }}"/>
{% endif %}
</div>

<!-- YOU CAN DELETE FROM THIS LINE -->

<div style="background-color:#fafafa;width:100%;text-aligh:center;">
<h1>Thanks for using the affiliate website template</h1>
<p>To take completely advantage of this template, a few simple steps are required and described in the documentation : http://bolt-affiliate-website-template.readthedocs.io/en/latest/configure_template.html</p>
<p>Concider as well reading the following page to get an overview of the different website architectures that are available : http://bolt-affiliate-website-template.readthedocs.io/en/latest/website_architecture.html</p>
<p>To delete this message, in the dashboard in the left menu, go to File management -> View/edit templates -> affiliatews/ -> index.twig. In the text editor, you can delete this message.</p>
</div>

<!-- TO THIS LINE INCLUDED -->

{% endblock banner %} 

{% block category %}
    <div class="tiles">
        {% for item in records %}
            {% include 'partials/_product.twig' %}
        {% endfor %}
    </div>
   {{ pager(template = 'partials/_sub_pager.twig') }}
{% endblock category %}
