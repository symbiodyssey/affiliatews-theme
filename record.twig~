{% extends 'partials/_master.twig' %}

{% block category %}
{% set ws_mode = config.get('theme/ws_mode') %}
{% set ws_ga = config.get('theme/ws_ga') %}
    <div class="tiles">
        {% set items = record.related() %}
        {% for item in items %}
            {% include 'partials/_product.twig' %}
        {% endfor %}
    </div>
{% endblock category %}
